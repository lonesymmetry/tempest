@Authors Adrian Hardt, Logan Traffas

Engine:
    -Database:
        -Add date functionality
        -Add sorting parsers (ie sort by type, date, etc.)
    -Get input from UI (make this adaptable)

Desktop UI:
    -Create UI document
    -Add ability to create new Items
    -Add menus to improve usability
    -Change it so we don't have to re-construct the entire root pane every time we change something

Android UI:
    -Shift code base to Android
    -Migrate UI to Android